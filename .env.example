# Data Download Configuration
VECM_PRICE_DOWNLOAD=auto  # Options: auto, force, off
VECM_INPUT=vecm_project/data/adj_close_data.csv

# Execution Configuration
VECM_MAX_WORKERS=4
VECM_STAGE=stage1  # Options: stage1, stage2, stage3
VECM_PREFILTER=off  # Options: on, off

# Trading Fees (optional overrides)
# PLAYBOOK_FEE_BUY=0.001
# PLAYBOOK_FEE_SELL=0.001

# Telegram Notifications (optional)
# TELEGRAM_BOT_TOKEN=123456:ABCDEF
# TELEGRAM_CHAT_ID=987654321

# Email Notifications via SMTP (optional)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=alerts@example.com
# SMTP_PASS=yourpassword
# SMTP_TO=trader@example.com
# SMTP_FROM=alerts@example.com
# SMTP_STARTTLS=true
